#!/bin/bash

#read -p "Command: " inputline
#bash -c "$inputline"
# These lines are commented, because of the tast. In the exercise we should run command withour parameters, but with using this command we can run all commands

bash -c "$1"
